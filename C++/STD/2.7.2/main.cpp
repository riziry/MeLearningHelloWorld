#include <iostream>
#include "lesson.h"

using namespace std;

int main(){
	string ls_name = "Data Structure";
	string ls_code = "STD";
	lesson les = create_lesson(ls_name, ls_code);
//	disp_lesson(les);
}
